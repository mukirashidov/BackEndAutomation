def before_scenario(context, scenario):
    # tags and hooks can be integrated in these methods
    """
    if <tagName> in scenario.tags:

        do something

    """
    pass


def after_scenario(context, scenario):
    # tags and hooks can be integrated in these methods
    """
    if <tagName> in scenario.tags:

        do something

    """
    pass
